# To-Do Task App

Simple Flask To-Do application that supports Create, Read, Edit, and Delete (CRED) operations and exposes simple REST-like endpoints for programmatic access.

## Features

* Add tasks via HTML form or API
* List tasks with created date
* Edit and delete tasks
* Minimal, responsive UI (SCSS provided)

